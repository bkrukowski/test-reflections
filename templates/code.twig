{{ '<' }}?php

/**
 * Generated by php-header-file/php-header-file
 * @url https://github.com/php-header-file/php-header-file
 */

namespace
{
    throw new \RuntimeException('File generated by php-header-file, only for IDE purposes, do not execute this file');
}

{#- classes \ReflectionClass[] -#}
{#- functions \ReflectionFunction[] -#}
{#- constants array -#}

{%- import 'classes.macros.twig' as classesMacros %}
{%- import 'functions.macros.twig' as functionsMacros %}
{%- import 'constants.macros.twig' as constantsMacros -%}

{%- if classes or functions or constants -%}
    {{- '\n\n' -}}
{%- endif -%}
{{- classesMacros.exportClasses(classes) -}}

{%- if classes and functions -%}
    {{- '\n\n' -}}
{%- endif -%}
{{- functionsMacros.exportFunctions(functions) -}}

{%- if (classes or functions) and constants -%}
    {{- '\n\n' -}}
{%- endif -%}
{{- constantsMacros.exportConsts(constants) -}}

{%- if classes or functions or constants -%}
    {{- '\n' -}}
{%- endif -%}
